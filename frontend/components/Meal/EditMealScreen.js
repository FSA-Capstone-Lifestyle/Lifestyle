import {
  Box,
  Flex,
  Pressable,
  Text,
  ScrollView,
  Button,
  Image,
  Divider,
  Container,
} from "native-base";
import React, { useEffect } from "react";
import { useDispatch, useSelector } from "react-redux";
import { fetchMeal } from "../../store/slices/singleMeal.slice";

const EditMealScreen = (props) => {
  return <Text>HELLO EDIT PAGE</Text>;
};

export default EditMealScreen;
